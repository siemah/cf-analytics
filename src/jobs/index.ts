import { Env } from "../types/global";

export default async function saveStatsToDB(env: Env) {
  // todo: list new stats
  // todo: create a D1 database
  // todo: bind it to the current worker
  // todo: sql query to create a DB if not exists
  // todo: save a new stats to the DB
}
